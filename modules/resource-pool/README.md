# Resource pool module

Creates a resource pool beneath a cluster, host, or another resource pool. Pass the resource pool's standard tag set from the tagging module through `tags`. Reservations guarantee capacity; limits cap usage even when spare capacity exists, so use limits only when there is a clear operational requirement.
