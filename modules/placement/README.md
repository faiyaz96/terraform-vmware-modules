# Placement module

Manages VM affinity, VM anti-affinity, and VM-to-host placement rules for an existing or newly managed compute cluster. Rules are advisory by default; set `mandatory = true` only after capacity and failure scenarios have been reviewed.
