# Compute cluster module

Creates a vSphere compute cluster and configures core DRS, DPM, HA, admission-control, tags, and host-membership settings. Destructive host evacuation remains disabled by default.

Existing production clusters should be imported before this module is applied.
