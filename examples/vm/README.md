# VM examples

- `linux/` demonstrates Linux hostname and domain customization.
- `windows/` demonstrates Windows Sysprep customization using a workgroup.
- `content-library/` demonstrates a content-library clone with an existing customization specification.

Both examples use the same VM module, tagging module, and security baseline.
