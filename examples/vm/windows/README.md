# Windows VM example

Clones one tagged Windows VM and customizes it with Sysprep. This example joins a workgroup; the VM module also supports Active Directory domain join using `join_domain`, `domain_admin_user`, and the sensitive `windows_domain_admin_password` input.
