# IAM example

Creates a least-privilege VM operator role and grants it to an existing identity-provider group on an existing vSphere entity. Roles and permissions do not support vSphere tag assignment.
